;; auto-generated
(defconst mu4e-mu-version "0.9.9.6pre3"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/home/cp/Downloads/mu"
  "Top-level build directory.")

(provide 'mu4e-meta)
