When reporting information to me, be extremely concise and sacrifice grammar for the sake of concision.

When you want to show me a file, open it in nvim in a new tmux window (e.g. `tmux new-window -n "filename" "nvim /path/to/file"`) instead of printing its contents.
