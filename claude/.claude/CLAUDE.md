When reporting information to me, be extremely concise and sacrifice grammar for the sake of concision.

When you want to show a file to the user then open it in nvim in a new tmux window (e.g. `tmux new-window -n "filename" "nvim /path/to/file"`) instead of printing its contents.

Use the same command when the users asks you to show him a file.
